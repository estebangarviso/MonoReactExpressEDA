import { name as producerAppName } from '../../apps/api-producer/package.json';

export const PDF_QUEUE = 'pdf:queue';
export const PDF_CHANNEL = 'pdf:channel';
export const PRODUCER_APP_NAME = producerAppName;
